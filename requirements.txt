# Core ML stack — see specs/001-manaca-instruct-tuning/research.md and plan.md's Technical Context
#
# Ranges below were updated after actually wiring up and running training/eval
# code against what a plain `pip install <pkg>` resolves to today (validated:
# torch 2.14.0+cu130, transformers 5.17.0, peft 0.20.0, trl 1.13.0,
# bitsandbytes 0.50.2, datasets 5.0.1, huggingface_hub 1.31.0). research.md §2's
# original ranges assumed an older ecosystem snapshot and would have pulled
# transformers 4.x / trl 0.9.x / datasets 2.x / huggingface_hub 0.x instead —
# incompatible with the dtype= kwarg (renamed from torch_dtype=) and trl's
# current SFTConfig/processing_class API that the code now actually uses.
torch>=2.3,<3.0            # install the CUDA build matching this machine's driver; see README for the exact index-url
transformers>=5.0,<6.0
peft>=0.11,<1.0
trl>=1.0,<2.0
bitsandbytes>=0.43,<1.0
datasets>=5.0,<6.0
pyyaml>=6.0,<7.0           # configs/train.yaml, configs/inference.yaml parsing

# Publishing (FR-009/FR-010)
huggingface_hub>=1.0,<2.0

# Grading (research.md §3) — only needed if difflib's stdlib performance is insufficient
rapidfuzz>=3.9,<4.0

# Dev/test
pytest>=8.0,<9.0
